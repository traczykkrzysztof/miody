<Window x:Class="APP.View.ResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ResultWindow" Height="600" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Image Grid.Column="0" Name="ResultImage" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
        <Menu Grid.Column="1" Background="WhiteSmoke">
            <MenuItem Header="Zapisz zdjęcie" />
            <MenuItem Header="Zapisz rezultat" />
            <MenuItem Header="Zakończ" Click="Close_Click" />
        </Menu>
        <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Name="textBlock1" TextWrapping="Wrap">
            Miara 1: <LineBreak />
            Miara 2: <LineBreak />
            Miara 3: <LineBreak />
        </TextBlock>
    </Grid>
</Window>
